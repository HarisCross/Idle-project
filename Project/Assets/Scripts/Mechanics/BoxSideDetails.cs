using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BoxSideDetails : MonoBehaviour {

    //holds details which are passed to boxSideController
    //public enum SideType { Input, Output, BonusModifier }
    //public SideType sideType = SideType.Input;
    public string side = "Side";
    public float IncomeHeld = 0;
    public float IncomeRate = 0f;


    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
